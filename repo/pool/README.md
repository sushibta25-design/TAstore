# TAstore package pool

Installable .deb files will be published here after their device targets and release builds are verified.

The flat APT index lives beside this folder in Packages, Packages.gz, and Release. The current index is intentionally empty.
